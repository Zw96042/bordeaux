# Bordeaux examples

Direct BDX writer examples and independent expected-byte fixtures live in `labview/tests/fixtures/binary`. These fixtures contain synthetic command metadata and are not robot execution evidence. See [LabVIEW integration](../docs/labview/index.md).
