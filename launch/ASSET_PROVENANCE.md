
- Wine-glass mark: `build/icon-assets/wine-glass.svg` and `build/icon.png`. Full-color launch lockups embed the canonical SVG source directly; light-background and one-color files are contrast adaptations of the same geometry.
- App icon: `build/icon.png`
- Code-built product visuals: authored for this launch kit from the behavior and visual structure of the Bordeaux renderer
- Deterministic launch exports, including the SVG/PNG brand-system board: generated by `launch/generate-assets.mjs`
- Video motion draft: generated by `launch/render-videos.sh` from the **Spill / Route** startup master, launch cards, and code-built product poster. A static title card is used only when the startup master is absent.
- Startup motion masters: authored as modeled, lit, and motion-blurred Blender 5.2 Eevee scenes by `prototypes/startup-animations/render-blender.py`, then parallel-rendered and encoded by `render-animations.mjs`. Each direction includes MP4, WebM, poster, manifest metadata, and an eight-frame review sheet.

## Campaign key art

- `assets/generated/bordeaux-launch-key-art.png` — deterministically extracted from frame 78 of the reviewed **Spill / Route** production master, extended to 1920×1080 with a blurred edge fill, then lightly graded and vignetted by `launch/generate-assets.mjs`. It preserves the same gravity-level fill, correct emitting rim, stream handoff, and floor contact reviewed in the motion master.

## Chap mascot

The current startup masters and launch exports contain no Chap artwork. If the character concept is revived, document ownership and public-distribution permission before adding any mascot source or render to the release kit.

## Fonts

The marketing site bundles the official Google Fonts Latin variable WOFF2 responses for Space Grotesk 400–700 and JetBrains Mono 400–700. The Blender scenes bundle the corresponding variable TrueType sources. Each font directory records exact upstream URLs and SHA-256 hashes and carries the copyright notices plus the full SIL Open Font License 1.1 text. Re-run the hash checks and update those records if a binary is replaced.

## Product and competition marks

No FIRST, FRC event, field sponsor, vendor, or team marks are included in the campaign assets. “FRC” is used descriptively. Confirm any trademark attribution required by the final distribution channel.
